# Agent 记忆系统框架设计

> 版本：v1.0（2026-08-22）
> 性质：个人 agent 记忆系统的设计文档，非文献综述。概念词汇（工作记忆 / 情景 / 语义 / 程序记忆）借自 CoALA（arXiv:2309-02427），分层与水位的工程思路参考 MemGPT（arXiv:2310.08560）。

## 一、总览

记忆系统分四层，按信息生命周期从短到长排列：

```
┌─────────────────────────────────────────────────────────┐
│ prompt（每轮真正发给 LLM 的内容，最小、最快、最易失）      │
│   = 系统提示 + 工作记忆 + 早期摘要 + 近期完整对话 + 当前指令 │
└─────────────────────────────────────────────────────────┘
        ↑ 每轮组装                ↑ 驱逐/摘要时抢救
┌──────────────────────┐  ┌───────────────────────────────┐
│ 短期记忆（会话级）     │  │ 工作记忆（任务级）              │
│ 当前会话的完整对话记录 │  │ 当前任务的状态结构（变量、决策、 │
│ = agent 原生会话日志  │  │ TODO、未完成任务标记）          │
└──────────────────────┘  └───────────────────────────────┘
        ↑ 会话结束归档+蒸馏        ↑ 会话结束：完成则蒸馏后清除
┌─────────────────────────────────────────────────────────┐
│ 长期记忆（跨会话、跨任务）                                 │
│   情景记忆 × 语义记忆 × 程序记忆                           │
│   全局 / 项目 / agent 三种作用域                           │
└─────────────────────────────────────────────────────────┘
```

核心原则：**三层记忆对应三种生命周期**——短期记忆随会话滚动，工作记忆随任务存灭，长期记忆跨任务持久。prompt 不是一层记忆，而是各层记忆每轮组装出的快照。

## 二、各层定义

### 2.1 长期记忆

落盘持久存储，按「类型 × 作用域」两个维度组织。

**三种类型：**

| 类型 | 含义 | 例子 |
|---|---|---|
| 情景记忆（Episodic） | 具体事件和经历的记录 | 「用户订了下周五去东京的 ANA 航班」——有具体时间、对象、细节 |
| 语义记忆（Semantic） | 从事件中抽象出的稳定知识 | 「用户是素食者」「用户偏好靠窗座位」——从多次交互提炼的稳定特征 |
| 程序记忆（Procedural） | 行为模式和流程 | 从反复订机票中学到的流程：「搜索直飞→确认座位偏好→用常旅客号码→订餐」 |

**三种作用域：**

- **全局（global）**：对所有项目、所有 agent 成立的内容（用户的通用偏好、习惯）；
- **项目（project）**：只与某个项目相关的事实和约定（技术选型、目录结构、项目规范）；
- **agent**：只与某个 agent 自身行为相关的记忆。

**写入时机与方式：** 会话结束时（判定规则见第四节），通过 MCP 调用记忆服务，以当前会话的完整对话记录（短期记忆）和工作记忆为输入，执行两个动作：

1. **归档**：对话原文归入情景记忆（原样保存，不改写）；
2. **蒸馏**：从对话和工作记忆中提炼出值得长期保存的语义记忆与程序记忆，写入对应作用域；与既有记忆冲突时以新内容为准更新旧条目，而不是追加重复条目。

### 2.2 工作记忆

**定位：** 当前任务的持久状态结构——任务目标、已确认的决策、变量当前值、TODO、未完成任务标记。它比短期历史活得久（不怕窗口驱逐），比长期记忆活得短（任务结束即清除）。

**作用域：** 统一为项目作用域，整个项目只维护一份。

**生命周期：**

1. **会话开始**：从持久化的工作记忆文件加载；新任务则空白创建；
2. **会话进行中**：LLM 认为状态有变化时可随时更新；每三轮强制检查一次是否需要更新；
3. **会话结束**：随长期记忆同步做一次清理——已完成的任务，有价值结论蒸馏进长期记忆后从工作记忆清除；未完成的任务保留原样，下次会话开始时重新加载。

设计要点：工作记忆**不随会话结束删除**。会话边界不等于任务边界，跨会话的任务状态若被迫并入长期记忆，类型上不合身（既不是稳定知识也不是已结束事件），还会污染长期记忆、增加检索失败的环节。

### 2.3 短期记忆

**定位：** 当前会话的完整对话记录。

**载体：** 不新建专门文件，直接复用 agent 运行时的原生会话日志（如 Kimi Code 的 `wire.jsonl`），保证单一真相来源、最高保真度（含工具调用的输入输出）、零双写维护成本。

**读取适配层：** 原生日志是机器格式（混有系统提示、工具调用、元数据），不适合直接检索。在日志与检索/摘要逻辑之间设一个读取适配器，负责把原始日志解析成干净的轮次序列（用户输入、agent 回复、工具调用）；上层逻辑只面对适配器输出。更换 agent 运行时时只需改适配器。

**作用域：** agent 作用域、按会话分区索引。对话原文只对产生它的 agent 和会话有直接意义，不设全局级和项目级短期记忆；跨 agent、跨项目需要复用的内容一律经蒸馏进长期记忆后共享。

## 三、读取路径

### 3.1 每轮 prompt 的组装

按以下顺序组装（重要信息占首尾，依据 Lost in the Middle 的 U 型曲线实证）：

```
① 系统提示（含工具、skills 元数据）
② 工作记忆（当前任务状态结构）
③ 早期对话的递归摘要（超过水位线的部分，见 3.2）
④ 近期对话的完整原文（至少保留最近三轮）
⑤ 当前轮次的用户指令
```

### 3.2 摘要触发（token 水位制）

- 以 prompt 占用达到上下文窗口的 **20%** 为水位线，超过则把较早期内容提炼为递归摘要；
- 无论水位如何，**至少保留最近三轮对话的完整原文**；
- 触发摘要前做一次「抢救检查」：将被压缩内容中尚未写入工作记忆的关键状态（如变更过的约束条件）先写入工作记忆，再做摘要——递归摘要是有损的，关键状态不能只靠摘要兜底。

### 3.3 查询路由（prompt 缺失内容时）

按问题类型选择检索入口，命中即停：

| 问题类型 | 检索顺序 |
|---|---|
| 「当前是什么」（状态类） | 工作记忆 → 短期记忆 → 长期记忆 |
| 「当时说了什么」（原文类） | 短期记忆 → 长期记忆（情景） |
| 「以前怎么做的」（经验类） | 长期记忆（语义/程序）→ 短期记忆 |

**新鲜度补偿规则：** 状态类查询先查工作记忆时，若工作记忆在上一轮结束后没有更新过，必须额外读取短期记忆里「上次工作记忆更新之后」的全部内容，确认没有更新的状态变更。实现上给工作记忆维护一个「最后更新到第几轮」的水位标记，补偿读取从水位之后开始。此规则只对状态类查询启用，其他查询类型不触发，避免浪费 token。

## 四、会话边界与写入路径

### 4.1 会话结束的判定（优先级从高到低）

1. **人工明确结束**（黄金标准）：用户显式表示「今天到这」「结束」等；
2. **十分钟无活动**：兜底判定；
3. **未完成任务否决**：触发判定前先检查工作记忆，若存在标记为「未完成」的任务，则不视为结束，不触发长期记忆写入。

### 4.2 各级写入路径汇总

| 记忆层 | 写入时机 | 写入内容 | 写入方 |
|---|---|---|---|
| 短期记忆 | 每轮自动 | 完整对话原文（由运行时自动记录） | agent 运行时 |
| 工作记忆 | LLM 不定期 + 每三轮强制检查 + 摘要前抢救 | 任务状态结构的增改 | LLM 自主决策 |
| 长期记忆 | 会话结束时（MCP 调用） | 原文归档（情景）+ 蒸馏提炼（语义/程序），冲突时更新旧条目 | 记忆服务（蒸馏管线） |

## 五、作用域矩阵

| 记忆层 | 全局 | 项目 | agent | 会话分区 |
|---|---|---|---|---|
| 长期记忆（情景/语义/程序） | ✓ | ✓ | ✓ | — |
| 工作记忆 | — | ✓（唯一一份） | — | — |
| 短期记忆 | — | — | ✓ | ✓ |

## 六、已知风险与对策

| 风险 | 对策 |
|---|---|
| 递归摘要逐轮丢失细节 | 摘要前抢救检查 + 关键状态进工作记忆兜底 |
| 工作记忆过期（任务完成未清理） | 会话结束时的状态清理纪律（完成即蒸馏清除） |
| 十分钟定时器误切长思考中的会话 | 未完成任务否决 + 人工显式结束优先 |
| 检索命中过时的流水原文 | 状态类查询工作记忆优先 + 新鲜度补偿规则 |
| 更换 agent 运行时导致日志格式失效 | 读取适配层隔离格式变化 |
